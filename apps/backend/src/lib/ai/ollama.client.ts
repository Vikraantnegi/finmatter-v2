/**
 * Ollama client for local LLM calls (categorization fallback, parsing assistance).
 * Used only for non–money-decision tasks; reward math stays deterministic.
 */

// Placeholder. Implement when adding AI-assisted categorization/parsing.
