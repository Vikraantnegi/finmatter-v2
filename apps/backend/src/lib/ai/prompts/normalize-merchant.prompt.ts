/**
 * Prompt for normalizing merchant name (raw description → clean merchant name).
 * Used when rules do not match; AI suggests normalized name only.
 */

// Placeholder. Implement when adding AI merchant normalization.
