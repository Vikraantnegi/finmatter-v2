/**
 * Prompt for categorizing a transaction into SpendCategory (rules fallback).
 * AI suggests category only; final assignment can be overridden by rules.
 */

// Placeholder. Implement when adding AI categorization fallback.
